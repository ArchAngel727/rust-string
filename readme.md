### Rust library containing String implementation

### Usage
After copying into crate import using `crate::string::String;`

#### Tests
Run tests using `cargo test`

#### Docs
Generate documentation using `cargo doc` and open using `cargo doc --open`
